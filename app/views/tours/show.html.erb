<div id="tourDetails">
<p id="notice"><%= notice %></p>

<%= render 'tours/tour_detail', :tour => @tour %>
<section id="ratings">
  <%= render 'ratings/existing_and_new', :tour => @tour %>
</section>

</div>


<script type="text/javascript">
    //Grab the tour data...there is definitely an easy way to do this, I just don't know rails well enough
    var id = <%= @tour.id %>;
    $.ajax({
      type: 'GET',
      url: "/tours/"+id+".json",
      success: function (t) {loadMap($("div.map").get(0),{}, t.stops)},
      error: function () {}
    });
    
</script>

